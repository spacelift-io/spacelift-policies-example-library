name: Trigger Dependencies Via Labels
source: trigger-dependencies-via-labels.rego
type: trigger
description: |
  This policy automates triggering dependencies based on labels and state information. It allows for sophisticated
  dependency management within Spacelift, ensuring that infrastructure changes are propagated correctly across
  dependent stacks.
labels:
- trigger
- dependencies
- labels
- state
